fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1
networkx==3.3
beautifulsoup4==4.12.3
httpx==0.27.2

# RAG retrieval (services/rag.py) — exact cosine over the in-process chunk
# matrix. numpy is deliberate: at ~400 chunks brute-force beats any vector DB
# on both latency and ops cost; see docs/RAG.md "Why no vector database".
numpy>=1.26

# database
sqlalchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.10

# auth (Phase 2)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator==2.2.0

# tests
pytest==8.3.3
