services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: systemsim
      POSTGRES_PASSWORD: systemsim
      POSTGRES_DB: systemsim
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
